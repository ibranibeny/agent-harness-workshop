# Agent Harness Workshop

- Node.js 24+ on Windows. Use npm.cmd. No Python, az CLI, WSL or containers.
- Real Foundry inference only. Never fabricate tool results.
- Keep .env, MCP configuration, credentials and runtime outside Git.
- Preserve validation, tool allowlists, exact human approval and bounded conversation history.
- npm.cmd test is deterministic; npm.cmd run test:live is billable.
- WorkIQ writes are not connected. Do not infer runtime authorization from editor integrations.
